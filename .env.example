# Copy to .env and add ONE of the two keys. Do not commit .env to Git/GitLab.

# Option 1 – FREE (recommended for development/demo)
# Get a key at https://console.groq.com/keys
GROQ_API_KEY=gsk_...

# Option 2 – OpenAI (paid, new accounts may get initial credit)
# Get a key at https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...

PORT=3001

# Optional: limit chat to one topic (e.g. programming, cooking, travel). Empty = no limit.
# CHAT_TOPIC=programming

# Optional: path to a file with company info so the AI can answer precisely about your business. Leave unset or empty to disable. See docs/company-context.md.
# COMPANY_KNOWLEDGE_PATH=knowledge/company.md
