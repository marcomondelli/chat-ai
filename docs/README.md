# Docs

Place **screenshot.png** here (a capture of the chat UI) so the main [README](../README.md) can display it. Create the image by running the app, opening http://localhost:5173, and taking a screenshot.
